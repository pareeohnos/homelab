---
- name: reload systemd
  become: true
  systemd:
    daemon_reload: true

- name: restart immich
  become: true
  command: docker compose up -d --force-recreate
  args:
    chdir: "{{ immich_datadir }}"
